# resources/

Place `app_icon.ico` here to brand the window and the PyInstaller-built
`.exe`. Both `main.py` and `build.spec` pick it up automatically if
present; the app runs fine without it (falls back to the default icon).
